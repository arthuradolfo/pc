/* recursion:  in the bss address */
int x;

int fatorial (int n)
{
    if (n == 1) then {
       return n;
    };
    return fatorial(n-1) * n;          
}

int main()
{
  int y;
  y = 10;
  x = fatorial(y);
}
