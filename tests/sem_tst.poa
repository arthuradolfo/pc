class tipo1_t [ public int integer ];
class tipo2_t [ public int integer2 ];

tipo1_t t0_v[1];
tipo1_t t1_v[1];
tipo2_t t2_v[1];

bool funk(int a)
{
  //
}

bool func(bool b1, bool b2, float f1, float f2, int i1, int i2)
{
  func(true,false,1.1,1.2,0,1);
  func(1,0,1.2,1.1,false,true);

  main(i1);
  main(b1);
  main(f1);
}

tipo2_t main(int a0)
{
  tipo1_t a;
  int b;
  int c <= 1;
  static tipo1_t d;
  static int e;
  static int f <= 1;
  const tipo1_t d;
  const int e;
  const int f <= 1;
  static const tipo1_t d;
  static const int e;
  static const int f <= 1;


  if (true) then
  {
    int dentro_if;
  };

  if (false) then
  {
    int dentro_if_else;
  };

  while ('a') do
  {
    int dentro_while_do;
  };

  do
  {
    int dentro_do_while;

  } while ('b');

  switch ('c')
  {
    int dentro_do_switch;
  };

  for (char d_var <= 'd' : true : d_var = 'e')
  {
    int dentro_do_for;
  };

  int fora_foreach;
  foreach(fora_foreach : 1,2,3,4)
  {
    int dentro_do_foreach;
  };


}
