class tipo1_t [ public int integer ];
class tipo2_t [ public int integer2 ];

tipo1_t t0_v[1];
tipo1_t t1_v[1];
tipo2_t t2_v[1];

bool funk(int a)
{
  //
}

bool func(bool b1, bool b2, float f1, float f2, int i1, int i2)
{
  func(true,false,1.1,1.2,0,1);
  func(1,0,1.2,1.1,false,true);
}

tipo2_t main(int a0, int a1, int a2)
{
  tipo1_t a;
  int b;
  int c <= 1;
  static tipo1_t d;
  static int e;
  static int f <= 1;
  const tipo1_t g;
  const int h;
  const int i <= 1;
  static const tipo1_t j;
  static const int k;
  static const int l <= 1;


  if (true) then
  {
    int dentro_if;
  };

  if (false) then
  {
    int dentro_if_else;
  };

  while ('a') do
  {
    int dentro_while_do;
  };

  do
  {
    int dentro_do_while;

  } while ('b');

  switch ('c')
  {
    int dentro_do_switch;
  };

  for (char d_var <= 'd' : true : d_var = 'e')
  {
    int dentro_do_for;
  };

  int fora_foreach;
  foreach(fora_foreach : 1,2,3,4)
  {
    int dentro_do_foreach;
  };

  main(a0, a1, a2);
}
